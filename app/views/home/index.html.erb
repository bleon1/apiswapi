<!DOCTYPE html>
<html lang="en" dir="ltr">
  <body>
    <main>
      <div class="container">
        <div class="row">
          <div class="column">
            PELICULA
          </div>
          <div class="column">
            EPISODIO
          </div>
          <div class="column">
            AÑO DE LANZAMIENTO
          </div>
          <div class="column">
            DIRECTOR
          </div>
          <div class="column">
            PRODUCTORES
          </div>
        </div>
          <% @response.each do |pelicula| %>
            <div class="row">
              <div class="column">
                <%= link_to pelicula['title'], movie_path(pelicula['episode_id']) %>
              </div>
              <div class="column">
                <%= pelicula['episode_id'] %>
              </div>
              <div class="column">
                <%= pelicula['created'].to_time.strftime('%Y') %>
              </div>
              <div class="column">
                <%= pelicula['director'] %>
              </div>
              <div class="column">
                <%= pelicula['producer'] %>
              </div>
            </div>
          <% end %>
      </div>
    </main>
  </body>
</html>